# Local dev config for ./run.sh — copy this file to ".env" and edit.
# The .env is gitignored and only affects local runs.

# Mode A: point the debugger at a different repo to scan as its root.
# Absolute path recommended. Leave unset/commented to scan the debugger repo itself.
SWARM_DEBUG_ROOT=/absolute/path/to/other-repo
